# Maps a chart family (config/chart-families.yaml) to the workflow
# that should run in the chart's owning repo once QA passes.

rancher-backup:
  repo: rancher/backup-restore-operator
  workflow: unrc.yaml
