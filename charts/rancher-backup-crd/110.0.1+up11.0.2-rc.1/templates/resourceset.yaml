---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: resourcesets.resources.cattle.io
spec:
  group: resources.cattle.io
  names:
    kind: ResourceSet
    listKind: ResourceSetList
    plural: resourcesets
    singular: resourceset
  scope: Cluster
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          controllerReferences:
            default: []
            description: ControllerReferences lists controllers to scale down during
              restore operations.
            items:
              description: ControllerReference identifies a controller to scale down
                during restore operations.
              properties:
                apiVersion:
                  description: APIVersion of the controller resource (e.g., "apps/v1")
                  type: string
                name:
                  description: Name of the controller
                  type: string
                namespace:
                  description: Namespace where the controller lives
                  type: string
                replicas:
                  description: Populated at restore time from the live cluster
                  format: int32
                  type: integer
                resource:
                  description: Resource type (e.g., "deployments", "statefulsets")
                  type: string
              required:
              - apiVersion
              - name
              - namespace
              - resource
              type: object
            type: array
            x-kubernetes-list-type: atomic
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          resourceSelectors:
            default: []
            items:
              properties:
                apiVersion:
                  type: string
                excludeKinds:
                  items:
                    type: string
                  type: array
                  x-kubernetes-list-type: set
                excludeResourceNameRegexp:
                  type: string
                fieldSelectors:
                  additionalProperties:
                    type: string
                  description: Set is a map of field:value. It implements Fields.
                  type: object
                kinds:
                  items:
                    type: string
                  type: array
                  x-kubernetes-list-type: set
                kindsRegexp:
                  type: string
                labelSelectors:
                  description: |-
                    A label selector is a label query over a set of resources. The result of matchLabels and
                    matchExpressions are ANDed. An empty label selector matches all objects. A null
                    label selector matches no objects.
                  nullable: true
                  properties:
                    matchExpressions:
                      description: matchExpressions is a list of label selector requirements.
                        The requirements are ANDed.
                      items:
                        description: |-
                          A label selector requirement is a selector that contains values, a key, and an operator that
                          relates the key and values.
                        properties:
                          key:
                            description: key is the label key that the selector applies
                              to.
                            type: string
                          operator:
                            description: |-
                              operator represents a key's relationship to a set of values.
                              Valid operators are In, NotIn, Exists and DoesNotExist.
                            type: string
                          values:
                            description: |-
                              values is an array of string values. If the operator is In or NotIn,
                              the values array must be non-empty. If the operator is Exists or DoesNotExist,
                              the values array must be empty. This array is replaced during a strategic
                              merge patch.
                            items:
                              type: string
                            type: array
                            x-kubernetes-list-type: atomic
                        required:
                        - key
                        - operator
                        type: object
                      type: array
                      x-kubernetes-list-type: atomic
                    matchLabels:
                      additionalProperties:
                        type: string
                      description: |-
                        matchLabels is a map of {key,value} pairs. A single {key,value} in the matchLabels
                        map is equivalent to an element of matchExpressions, whose key field is "key", the
                        operator is "In", and the values array contains only "value". The requirements are ANDed.
                      type: object
                  type: object
                  x-kubernetes-map-type: atomic
                namespaceRegexp:
                  type: string
                namespaces:
                  items:
                    type: string
                  type: array
                  x-kubernetes-list-type: set
                resourceNameRegexp:
                  type: string
                resourceNames:
                  items:
                    type: string
                  type: array
                  x-kubernetes-list-type: set
              required:
              - apiVersion
              type: object
            type: array
            x-kubernetes-list-type: atomic
        required:
        - resourceSelectors
        type: object
    served: true
    storage: true
    subresources:
      status: {}
