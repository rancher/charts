apiVersion: v1
kind: ConfigMap
metadata:
  namespace: {{ .Values.grafana.defaultDashboards.namespace }}
  name: {{ printf "%s-%s" (include "rancher-monitoring.name" $) "ingress-nginx" | trunc 63 | trimSuffix "-" }}
  {{- if .Values.grafana.sidecar.dashboards.annotations }}
  annotations: {{ toYaml .Values.grafana.sidecar.dashboards.annotations | nindent 4 }}
  {{- end }}
  labels:
    {{- if $.Values.grafana.sidecar.dashboards.label }}
    {{ $.Values.grafana.sidecar.dashboards.label }}: "1"
    {{- end }}
    app: {{ template "rancher-monitoring.name" $ }}-grafana
{{ include "rancher-monitoring.labels" $ | indent 4 }}
data:
{{ (.Files.Glob "files/ingress-nginx/*").AsConfig | indent 2 }}
