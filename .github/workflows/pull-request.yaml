name: CI-pullrequest

on:
  pull_request:
    branches:
      - integration-testing

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Validate
        run: sudo make validate
