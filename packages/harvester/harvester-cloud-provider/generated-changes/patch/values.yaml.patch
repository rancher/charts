--- charts-original/values.yaml
+++ charts/values.yaml
@@ -48,6 +48,11 @@
   pullPolicy: IfNotPresent
   # Overrides the image tag whose default is the chart appVersion.
   # tag: v0.2.8
+  # Explicitly uncomment and update this tag to ensure it pass upstrem `rancher/charts` check
+  # which complains:
+  # - orphanImage:  rancher/orphan-image, no tag → orphan_repository
+  tag: "v0.2.8"
+
 
 # Configuration Precedence:
 # 1. cloudConfig.secretName: Primary priority for new deployments. If explicitly set,
