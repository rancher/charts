url: https://kubernetes-charts.banzaicloud.com/charts/logging-operator-3.12.0.tgz
version: 100.0.0
additionalCharts:
  - workingDir: charts-crd
    crdOptions:
      templateDirectory: crd-template
      crdDirectory: templates
      addCRDValidationToMainChart: true
# Chart.yaml needed min rancher version, but chart was not ready for version bump. 
# Remove doNotRelease when chart is ready for version bump. 
doNotRelease: true
