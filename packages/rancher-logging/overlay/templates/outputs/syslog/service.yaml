{{- if .Values.syslog.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-syslog-forwarder
spec:
  selector:
    output: syslog
  ports:
    - protocol: TCP
      port: 24224
      targetPort: 24224
{{- end }}
